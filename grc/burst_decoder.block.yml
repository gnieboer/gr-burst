# auto-generated by grc.converter

id: burst_decoder
label: Burst FEC Decoder
category: burst

parameters:
-   id: code
    label: FEC Code Def
    dtype: raw
    default: None

inputs:
-   domain: message
    id: fpdus
    optional: true

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import burst
    make: burst.cpdu_matlab_writer()

file_format: 1
