# auto-generated by grc.converter

id: burst_framer
label: Burst Framer
category: burst

parameters:
-   id: bs
    label: Blocksize
    dtype: int
    default: '1024'

inputs:
-   domain: message
    id: packed_pdus
    optional: true

outputs:
-   domain: message
    id: unpacked_pdus
    optional: true

templates:
    imports: import burst
    make: burst.framer(${bs})

file_format: 1
