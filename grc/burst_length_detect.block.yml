# auto-generated by grc.converter

id: burst_length_detect
label: Burst Length Detector
category: burst

inputs:
-   domain: message
    id: cpdus
    optional: true

outputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import burst
    make: burst.length_detect()

file_format: 1
