# auto-generated by grc.converter

id: burst_padder
label: Burst Padder
category: burst

parameters:
-   id: blocksize
    label: Block Size Bits
    dtype: int
    default: '144'

inputs:
-   domain: message
    id: pdus
    optional: true

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import burst
    make: burst.padder(${blocksize})

file_format: 1
