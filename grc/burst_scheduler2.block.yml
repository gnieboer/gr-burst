# auto-generated by grc.converter

id: burst_scheduler2
label: Burst Scheduler2
category: burst

parameters:
-   id: fs
    label: Sample Rate
    dtype: real
    default: samp_rate

inputs:
-   domain: message
    id: sched_pdu
    optional: true
-   domain: message
    id: nproduced
    optional: true

outputs:
-   domain: message
    id: sched_pdu
    optional: true

templates:
    imports: import burst
    make: burst.burst_scheduler2(${fs})

file_format: 1
