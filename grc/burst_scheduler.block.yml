# auto-generated by grc.converter

id: burst_scheduler
label: Burst Scheduler
category: burst

inputs:
-   domain: message
    id: sched_pdu
    optional: true
-   domain: message
    id: nproduced
    optional: true

outputs:
-   domain: message
    id: sched_pdu
    optional: true

templates:
    imports: import burst
    make: burst.burst_scheduler()

file_format: 1
