# auto-generated by grc.converter

id: burst_length_detect_c
label: Burst Length Detector (C++)
category: burst

inputs:
-   domain: message
    id: cpdus
    optional: true

outputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import burst
    make: burst.length_detect_c()

file_format: 1
