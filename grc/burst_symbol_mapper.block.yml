# auto-generated by grc.converter

id: burst_symbol_mapper
label: symbol_mapper
category: burst

parameters:
-   id: mod_type
    label: Modulaton
    dtype: enum
    options: [mapper.BPSK, mapper.P2BPSK, mapper.QPSK, mapper.PSK8]
    option_labels: [BPSK, Pi/2BPSK, QPSK, 8PSK]
-   id: symbol_values
    label: Symbol Values
    dtype: int_vector
-   id: offset
    label: Phase Offset
    dtype: raw
    default: cmath.exp(0.25 * 1.0j * math.pi)

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: |-
        import burst
        import mapper
        import cmath
        import math
    make: burst.symbol_mapper(${mod_type}, ${symbol_values}, ${offset})

file_format: 1
