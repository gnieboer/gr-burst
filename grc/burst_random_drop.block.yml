# auto-generated by grc.converter

id: burst_random_drop
label: Burst Random Drop
category: burst

parameters:
-   id: p_drop
    label: p(drop) [0,1]
    dtype: float
    default: '0.1'
-   id: delay
    label: Delay(s)
    dtype: float
    default: '1.0'

inputs:
-   domain: message
    id: pdus
    optional: true

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import burst
    make: burst.random_drop(${p_drop}, ${delay})

file_format: 1
