# auto-generated by grc.converter

id: burst_interp_cpdu
label: Dynamic Interp/Synthesis
category: burst

parameters:
-   id: fs
    label: Sample Rate
    dtype: real
    default: samp_rate

inputs:
-   domain: message
    id: cpdus
    optional: true

outputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import burst
    make: burst.interp_cpdu(${fs})

file_format: 1
