# auto-generated by grc.converter

id: burst_synchronizer
label: Burst Feedforward PSK Symbol Synchronizer
category: burst

parameters:
-   id: sps
    label: Samples per Symbol
    dtype: float
    default: '2.0'

inputs:
-   domain: message
    id: cpdus
    optional: true

outputs:
-   domain: message
    id: cpdus
    optional: true
-   domain: message
    id: timing_metric
    optional: true
-   domain: message
    id: sym_timed
    optional: true
-   domain: message
    id: phase_ramp
    optional: true

templates:
    imports: import burst
    make: burst.synchronizer(${sps})

file_format: 1
