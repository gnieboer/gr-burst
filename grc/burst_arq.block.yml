# auto-generated by grc.converter

id: burst_arq
label: Burst ARQ
category: burst

parameters:
-   id: timeout
    label: Timeout (s)
    dtype: float
    default: '2.5'
-   id: max_attempts
    label: Max Attempts
    dtype: int
    default: '3'

inputs:
-   domain: message
    id: rx_in
    optional: true
-   domain: message
    id: tx_in
    optional: true

outputs:
-   domain: message
    id: rx_out
    optional: true
-   domain: message
    id: tx_out
    optional: true

templates:
    imports: import burst
    make: burst.arq(${timeout}, ${max_attempts})

file_format: 1
