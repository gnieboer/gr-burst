# auto-generated by grc.converter

id: burst_synchronizer_v3
label: Burst Feedforward QPSK Symbol Synchronizer (v3)
category: burst

parameters:
-   id: sps
    label: Samples per Symbol
    dtype: float
    default: '2.0'
-   id: fs
    label: Sample Rate
    dtype: float
    default: '100000'

inputs:
-   domain: message
    id: cpdus
    optional: true

outputs:
-   domain: message
    id: cpdus
    optional: true

templates:
    imports: import burst
    make: burst.synchronizer_v3(${sps}, ${fs})

file_format: 1
