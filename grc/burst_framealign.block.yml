# auto-generated by grc.converter

id: burst_framealign
label: Burst Frame Align
category: burst

parameters:
-   id: skip
    label: Skip
    dtype: int
    default: '24'
-   id: bs
    label: Blocksize
    dtype: int
    default: '1024'

inputs:
-   domain: message
    id: fpdus
    optional: true

outputs:
-   domain: message
    id: fpdus
    optional: true

templates:
    imports: import burst
    make: burst.framealign(${skip}, ${bs})

file_format: 1
