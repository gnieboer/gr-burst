# auto-generated by grc.converter

id: burst_correlator_filter
label: Correlator Filter
category: burst

parameters:
-   id: l1
    label: Mu MA Len
    dtype: int
    default: '100'
-   id: i1
    label: Sigma alpha
    dtype: real
    default: 1e-4
-   id: thresh
    label: Offset
    dtype: float
    default: '-10'

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import burst
    make: burst.correlator_filter(${l1}, ${i1}, ${thresh})

file_format: 1
